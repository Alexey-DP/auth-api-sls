export enum ErrorMessages {
  UNAUTHORIZED = "Authorization required!",
  WrongEmailOrPassword = "Wrong email or password!",
  WrongShortLink = "The link doesn't exists, check your link and try again",
}
